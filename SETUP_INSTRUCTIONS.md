# 🚀 Инструкции по настройке реальной работы

## Переменные окружения

Создайте файл `.env.local` в корне проекта со следующими переменными:

```env
# Supabase настройки
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Опционально: Ollama для AI функций
OLLAMA_API_URL=http://127.0.0.1:11434
```

## Получение Supabase ключей

1. Перейдите в [Supabase Dashboard](https://supabase.com/dashboard)
2. Создайте новый проект или выберите существующий
3. Настройки → API
4. Скопируйте:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon/public key** → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role key** → `SUPABASE_SERVICE_ROLE_KEY`

## Настройка базы данных

Убедитесь, что в вашей базе данных Supabase созданы таблицы:

```sql
-- Таблица пользователей
CREATE TABLE пользователи (
  идентификатор UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  электронная_почта VARCHAR NOT NULL UNIQUE,
  имя VARCHAR,
  фамилия VARCHAR,
  телефон VARCHAR,
  компания VARCHAR,
  должность VARCHAR,
  телеграмма VARCHAR,
  аватар_url TEXT,
  био TEXT,
  создано_в TIMESTAMPTZ DEFAULT NOW(),
  обновлено_в TIMESTAMPTZ DEFAULT NOW()
);

-- Таблица контактов
CREATE TABLE контакты (
  идентификатор UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  ID_пользователя UUID REFERENCES пользователи(идентификатор),
  имя VARCHAR,
  фамилия VARCHAR,
  компания VARCHAR,
  должность VARCHAR,
  примечания TEXT,
  linkedin_url TEXT,
  день_рождения DATE,
  источник VARCHAR DEFAULT 'manual',
  создано_в TIMESTAMPTZ DEFAULT NOW(),
  обновлено_в TIMESTAMPTZ DEFAULT NOW()
);

-- Таблица email контактов
CREATE TABLE контактные_электронные_почты (
  идентификатор UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  контактный_идентификатор UUID REFERENCES контакты(идентификатор),
  электронная_почта VARCHAR NOT NULL,
  этикетка VARCHAR DEFAULT 'work',
  is_primary BOOLEAN DEFAULT false
);

-- Таблица телефонов контактов
CREATE TABLE контактные_телефоны (
  идентификатор UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  контактный_идентификатор UUID REFERENCES контакты(идентификатор),
  телефон VARCHAR NOT NULL,
  этикетка VARCHAR DEFAULT 'work',
  is_primary BOOLEAN DEFAULT false
);
```

## Запуск проекта

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Проверка работы

1. Загрузите Excel/CSV файл с данными контактов
2. Система автоматически определит поля и предложит маппинг
3. Выполните импорт - данные попадут в вашу реальную базу Supabase

## Важные изменения

- ✅ Удалены моковые константы тестового пользователя
- ✅ Добавлена поддержка реальных ID пользователей
- ✅ Убраны отладочные логи из production кода
- ✅ Улучшена обработка ошибок
- ✅ Добавлена валидация данных перед импортом

Теперь система работает с реальными данными!
